import { OperationDirectory } from '../components/lobby/OperationDirectory'

/** The directory of operations (`OperationDirectory`), on its own route. */
export function OperationsPage() {
  return <OperationDirectory />
}
